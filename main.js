// Npm init to create package.json
// Install prompt for user input in the command line
// Create variable to require word.js file
// Create variable to require letter.js file

var prompt = require ('prompt');

var word = require ('./word.js');

var letter = require ('./letter.js');

console.log ('hello');
